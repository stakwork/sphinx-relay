kind: pipeline
type: docker
name: default

steps:
  - name: prepare-test-build
    image: sphinx-lightning/sphinx-relay-test 
    volumes:
    # - name: cache
    #   path: /artifacts
    

# - name: build-image
#   image: node
#   volumes:
#   - name: cache
#     path: /artifacts
#   commands:
#   - echo "TODO"
#
# - name: push-image
#   image: node
#   volumes:
#   - name: cache
#     path: /artifacts
#   commands:
#   - echo "TODO"

# volumes:
# - name: cache
#   temp: {}

trigger:
  repo:
    - stakwork/sphinx-relay
    - daniel-ro/sphinx-relay
  branch:
    - drone
    - master
