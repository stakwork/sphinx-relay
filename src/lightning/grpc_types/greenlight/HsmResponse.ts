// Original file: proto/greenlight.proto


export interface HsmResponse {
  'request_id'?: (number);
  'raw'?: (Buffer | Uint8Array | string);
}

export interface HsmResponse__Output {
  'request_id': (number);
  'raw': (Buffer);
}
